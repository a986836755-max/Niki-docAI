# Context: node
> @CONTEXT: Local | node | @TAGS: @LOCAL
> 最后更新 (Last Updated): 2026-02-24 15:10:58

## !RULE
<!-- Add local rules here -->

<!-- NIKI_AUTO_Context_START -->
## @STRUCTURE
*   **[main.d.ts](main.d.ts#L1)**: <reference types="node" />
    *   `@API`
        *   `PUB:` CLS **IPCMessageReader**
            *   `VAL->` VAR process
        *   `PUB:` CLS **IPCMessageWriter**
            *   `VAL->` VAR process
            *   `VAL->` VAR errorCount
            *   `VAL->` VAR handleError
        *   `PUB:` CLS **PortMessageReader**
            *   `VAL->` VAR onData
        *   `PUB:` CLS **PortMessageWriter**
            *   `VAL->` VAR port
            *   `VAL->` VAR errorCount
            *   `VAL->` VAR handleError
        *   `PUB:` CLS **SocketMessageReader**
        *   `PUB:` CLS **SocketMessageWriter**
            *   `VAL->` VAR socket
        *   `PUB:` CLS **StreamMessageReader**
        *   `PUB:` CLS **StreamMessageWriter**
        *   `PUB:` CLS **PipeTransport**
        *   `PUB:` CLS **SocketTransport**
*   **[main.js](main.js#L1)**
    *   `@API`
        *   `PUB:` CLS **IPCMessageReader**
            *   `PUB:` MET **constructor**`(process)`
            *   `PUB:` MET **listen**`(callback)`
        *   `PUB:` CLS **IPCMessageWriter**
            *   `PUB:` MET **constructor**`(process)`
            *   `PUB:` MET **write**`(msg)`
            *   `PUB:` MET **handleError**`(error, msg)`
            *   `PUB:` MET **end**`()`
        *   `PUB:` CLS **PortMessageReader**
            *   `PUB:` MET **constructor**`(port)`
            *   `PUB:` MET **listen**`(callback)`
        *   `PUB:` CLS **PortMessageWriter**
            *   `PUB:` MET **constructor**`(port)`
            *   `PUB:` MET **write**`(msg)`
            *   `PUB:` MET **handleError**`(error, msg)`
            *   `PUB:` MET **end**`()`
        *   `PUB:` CLS **SocketMessageReader**
            *   `PUB:` MET **constructor**`(socket, encoding = 'utf-8')`
        *   `PUB:` CLS **SocketMessageWriter**
            *   `PUB:` MET **constructor**`(socket, options)`
            *   `PUB:` MET **dispose**`()`
        *   `PUB:` CLS **StreamMessageReader**
            *   `PUB:` MET **constructor**`(readable, encoding)`
        *   `PUB:` CLS **StreamMessageWriter**
            *   `PUB:` MET **constructor**`(writable, options)`
        *   `PUB:` FUN **generateRandomPipeName**`()`
        *   `PUB:` FUN **createClientPipeTransport**`(pipeName, encoding = 'utf-8')`
        *   `PUB:` FUN **createServerPipeTransport**`(pipeName, encoding = 'utf-8')`
        *   `PUB:` FUN **createClientSocketTransport**`(port, encoding = 'utf-8')`
        *   `PUB:` FUN **createServerSocketTransport**`(port, encoding = 'utf-8')`
        *   `PUB:` FUN **isReadableStream**`(value)`
        *   `PUB:` FUN **isWritableStream**`(value)`
        *   `PUB:` FUN **createMessageConnection**`(input, output, logger, options)`
*   **[ril.d.ts](ril.d.ts#L1)**: <reference types="node" />
    *   `@API`
        *   `PUB:` CLS **RIL**
*   **[ril.js](ril.js#L1)**
    *   `@API`
        *   `PUB:` CLS **MessageBuffer**
            *   `PUB:` MET **constructor**`(encoding = 'utf-8')`
            *   `PUB:` MET **emptyBuffer**`()`
            *   `PUB:` MET **fromString**`(value, encoding)`
            *   `PUB:` MET **toString**`(value, encoding)`
            *   `PUB:` MET **asNative**`(buffer, length)`
            *   `PUB:` MET **allocNative**`(length)`
        *   `PUB:` CLS **ReadableStreamWrapper**
            *   `PUB:` MET **constructor**`(stream)`
            *   `PUB:` MET **onClose**`(listener)`
            *   `PUB:` MET **onError**`(listener)`
            *   `PUB:` MET **onEnd**`(listener)`
            *   `PUB:` MET **onData**`(listener)`
        *   `PUB:` CLS **WritableStreamWrapper**
            *   `PUB:` MET **constructor**`(stream)`
            *   `PUB:` MET **onClose**`(listener)`
            *   `PUB:` MET **onError**`(listener)`
            *   `PUB:` MET **onEnd**`(listener)`
            *   `PUB:` MET **write**`(data, encoding)`
            *   `PUB:` MET **callback**`(error)`
            *   `PUB:` MET **end**`()`
        *   `PUB:` FUN **setTimeout**`(callback, ms, ...args)`
        *   `PUB:` FUN **setImmediate**`(callback, ...args)`
        *   `PUB:` FUN **setInterval**`(callback, ms, ...args)`
        *   `PUB:` FUN **RIL**`()`
        *   `PUB:` FUN **install**`()`
<!-- NIKI_AUTO_Context_END -->
