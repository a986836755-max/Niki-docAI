# AI Context Guide (Niki-docAI)

> **Instructions for AI Assistants (Copilot, Cursor, Trae)**
> This file teaches YOU how to use the Niki-docAI toolchain to assist the user.
> **DO NOT EDIT THIS FILE**. It is auto-generated by `ndoc init`.

## 1. How to Read Context
*   **Project Structure**: Read `_MAP.md` first to understand the file tree.
*   **Architecture**: Read `_ARCH.md` and `_DEPS.md` to understand system design.
*   **Detailed Context**: Use `ndoc prompt <file> --focus` to get relevant code context (Vector Search).
*   **Code Skeleton**: Use `ndoc skeleton <file>` to see high-level API definitions without implementation details.

## 2. How to Verify Code
*   **Architecture Check**: Run `ndoc check` before modifying code to ensure no layer violations.
*   **Dependency Check**: Run `ndoc deps` to ensure no circular dependencies.
*   **Impact Analysis**: Run `ndoc impact` after modifying code to see which modules/tests are affected.

## 3. Workflow Protocol
1.  **Explore**: Start by reading `_MAP.md` and `_ARCH.md`.
2.  **Retrieve**: When User asks about a specific feature, use `ndoc prompt ... --focus`.
3.  **Implement**: Write code.
4.  **Verify**: Run `ndoc check` and `ndoc deps`.
5.  **Update**: Remind User to run `ndoc all` to update documentation.

## 4. Special Files
*   `_RULES.md`: Project configuration and architectural rules.
*   `_SYNTAX.md`: The definition of the Documentation Language used in this project.
*   `_STATUS.md`: Current project health and task board.
